"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var StrykerError_1 = require("../../src/StrykerError");
var chai_1 = require("chai");
var errors_1 = require("../../src/errors");
describe('StrykerError', function () {
    it('should set inner error', function () {
        var innerError = new Error();
        var sut = new StrykerError_1.default('some message', innerError);
        chai_1.expect(sut.innerError).eq(innerError);
    });
    it('should add inner error to the message', function () {
        var innerError = new Error();
        var sut = new StrykerError_1.default('some message', innerError);
        chai_1.expect(sut.message).eq("some message. Inner error: " + errors_1.errorToString(innerError));
    });
});
//# sourceMappingURL=StrykerError.spec.js.map